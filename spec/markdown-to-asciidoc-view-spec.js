'use babel';

import MarkdownToAsciidocView from '../lib/markdown-to-asciidoc-view';

describe('MarkdownToAsciidocView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
